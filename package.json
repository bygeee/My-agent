{
  "name": "ctf-agent",
  "version": "1.0.0",
  "private": true,
  "description": "TypeScript workspace for the z3gh0ne hub/backend refactor.",
  "workspaces": [
    "backend",
    "product"
  ],
  "scripts": {
    "dev": "npm run build --workspace backend && node backend/dist/cli.js",
    "cli": "npm run build --workspace backend && node backend/dist/cli.js",
    "build": "npm run build --workspaces --if-present",
    "start": "npm run build --workspace backend && node backend/dist/cli.js",
    "serve": "npm run serve --workspace backend",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "smoke": "npm run smoke --workspace backend"
  },
  "devDependencies": {
    "typescript": "^5.8.2"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/bygeee/My-agent.git"
  },
  "license": "ISC"
}
