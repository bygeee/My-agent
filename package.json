{
  "name": "ctf-agent",
  "version": "1.0.0",
  "private": true,
  "description": "TypeScript workspace for the z3gh0ne hub/backend refactor.",
  "bin": {
    "ctf-agent": "./scripts/ctf-agent.mjs"
  },
  "workspaces": [
    "backend",
    "product"
  ],
  "scripts": {
    "dev": "bun run backend/src/cli.ts chat",
    "cli": "bun run backend/src/cli.ts",
    "build": "bun build backend/src/cli.ts backend/src/index.ts backend/src/smoke.ts --target=bun --outdir backend/dist && bun build product/src/index.ts --target=bun --outdir product/dist",
    "start": "bun run backend/src/cli.ts chat",
    "serve": "bun run backend/src/index.ts",
    "typecheck": "bun x tsc --noEmit -p backend/tsconfig.json && bun x tsc --noEmit -p product/tsconfig.json",
    "smoke": "bun run backend/src/smoke.ts"
  },
  "devDependencies": {
    "typescript": "^5.8.2"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/bygeee/My-agent.git"
  },
  "license": "ISC"
}
